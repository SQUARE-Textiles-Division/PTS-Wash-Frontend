export default interface IndividualInOut {
    id: number;
    garment_unit: string;
    stage: string;
    action: string;
    rejection_reason: string;
    scanned_at: string;
    operator: string;
}